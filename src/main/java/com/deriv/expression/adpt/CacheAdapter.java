package com.deriv.expression.adpt;

/**
 * Adapter that connects an expression to a cache. This ensures that each Expression doesn't
 * store too much information.
 */
public interface CacheAdapter {
//  addToCache
}